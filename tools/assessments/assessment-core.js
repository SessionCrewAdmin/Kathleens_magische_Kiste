(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;root.AssessmentCoreV1=api})(typeof window!=='undefined'?window:globalThis,function(){'use strict';
function split(total,count){if(!Number.isInteger(total)||total<0||!Number.isInteger(count)||count<1)throw new RangeError('total and count must be non-negative integers');return Array.from({length:count},(_,i)=>Math.floor(total/count)+(i<total%count?1:0))}
function allocateAfbs(weights,count){const values=[Number(weights.I)||0,Number(weights.II)||0,Number(weights.III)||0],sum=values.reduce((a,b)=>a+b,0);if(sum!==100)return{sum,areas:Array(count).fill('II'),valid:false};const points=split(100,count);let cursor=0;return{sum,valid:true,areas:points.map(n=>{const mid=cursor+n/2;cursor+=n;return mid<=values[0]?'I':mid<=values[0]+values[1]?'II':'III'})}}
function validate({subject,tasks=[],duration_minutes=null,total_points=null,afb_weights=null,knowledge_base_status='available',curriculum_status='not_imported',scenario=''}){const warnings=[];if(!tasks.length)warnings.push('Der Assessment-Plan enthält noch keine Aufgaben.');if(knowledge_base_status!=='available')warnings.push('Fachliche Knowledge Base für diese Auswahl fehlt.');if(curriculum_status!=='available')warnings.push('LehrplanPLUS-KB nicht importiert; keine Kompetenzerwartungen zugeordnet.');if(subject==='Geschichte'&&!String(scenario).trim())warnings.push('Das für Geschichte erforderliche Rahmenszenario fehlt.');if(subject==='Geschichte'&&afb_weights){const sum=['I','II','III'].reduce((n,k)=>n+(Number(afb_weights[k])||0),0);if(sum!==100)warnings.push(`AFB-Verteilung ergibt ${sum} % statt 100 %.`)}for(const [i,t] of tasks.entries()){if(!t.expected_performance)warnings.push(`Aufgabe ${i+1}: erwartete Schülerleistung fehlt.`);if(!(Number(t.points)>0))warnings.push(`Aufgabe ${i+1}: Punkte müssen größer als 0 sein.`);if(!(Number(t.minutes)>0))warnings.push(`Aufgabe ${i+1}: Bearbeitungszeit muss größer als 0 sein.`);if(t.rubric?.length&&t.rubric.reduce((n,r)=>n+Number(r.points||0),0)!==Number(t.points))warnings.push(`Aufgabe ${i+1}: Teilpunkte im Erwartungshorizont stimmen nicht mit der Aufgabenpunktzahl überein.`);if(subject==='Geschichte'&&t.operator_expected_afb&&t.operator_expected_afb!==t.afb&&t.operator_expected_afb!=='I–III')warnings.push(`Aufgabe ${i+1}: Operator und AFB-Zuordnung weichen ab.`);if(subject==='Geschichte'&&t.kb_refs?.length&&!t.materials?.length)warnings.push(`Aufgabe ${i+1}: Quellenreferenz liegt vor, das Originalmaterial wurde nicht beigefügt.`);for(const m of t.materials||[])if(m.warning)warnings.push(`Aufgabe ${i+1}, Material ${m.name}: ${m.warning}`)}if(duration_minutes&&tasks.length&&tasks.reduce((n,t)=>n+Number(t.minutes||0),0)!==Number(duration_minutes))warnings.push('Aufgabenzeiten summieren sich nicht auf die Gesamtbearbeitungszeit.');if(total_points&&tasks.length&&tasks.reduce((n,t)=>n+Number(t.points||0),0)!==Number(total_points))warnings.push('Aufgabenpunkte summieren sich nicht auf die festgelegte Gesamtpunktzahl.');return[...new Set(warnings)]}
function totals(tasks=[]){return{task_count:tasks.length,total_points:tasks.reduce((n,t)=>n+Number(t.points||0),0),duration_minutes:tasks.reduce((n,t)=>n+Number(t.minutes||0),0)}}
return{split,allocateAfbs,validate,totals};
});
